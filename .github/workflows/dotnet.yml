name: .NET CI

on:
  push:
    branches: [ "main", "develop", "homologacao" ]
  pull_request:
    branches: [ "main", "develop", "homologacao" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependências
      run: dotnet restore GreenHome.sln

    - name: Build
      run: dotnet build GreenHome.sln --no-restore --configuration Release

    - name: Testes
      run: dotnet test GreenHome.sln --no-build --verbosity normal
